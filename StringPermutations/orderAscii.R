asciiSort <- function(lista) {
  for(i in 1:length(lista)){
    listOrder <- paste(listOrder, utf8ToInt(lista[i]))
  }
  for(i in 1:length(lista)){
    listOrder <- paste(listOrder, utf8ToInt(lista[i]))
  }
}

