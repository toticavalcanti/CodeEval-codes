Write a program which determines the largest prime palindrome less than 1000.

INPUT SAMPLE:

There is no input for this program.

OUTPUT SAMPLE:

Print to stdout the largest prime palindrome less than 1000.

929

-----------------------------------------------------------------------------

Escreva um programa que determina a maior palíndromo de números primos menor do que 1000.

ENTRADA DA AMOSTRA:

Não há entrada para este programa.

Exemplo de saída:

Imprime na saída padrão a maior palíndrome de números primos a menos de 1000.

929